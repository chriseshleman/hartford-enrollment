# Archived — two-chart version, 23 August 2026

The site as it stood when chart 2 (Hartford residents by school type) was live.

Pulled from the public page because the pre-2016 reconstruction isn't settled yet.
Nothing here is wrong as far as it goes; it just hasn't been reviewed by anyone
who knows the Sheff record well, and the compliance-rate-versus-enrolment
distinction needs more thought before it's worth publishing.

Contents:
- `index.html` — the full two-chart page
- `build_choice_panel.py` — the chart 2 pipeline
- `hartford_choice_panel.csv` — chart 2 data, 1986-2025
- `hartford_choice_review.xlsx` — the colour-coded review copy sent out for comment

To restore: copy `index.html` and `build_choice_panel.py` back to the project root
and re-run both build scripts. The chart 2 pipeline is unchanged and still works.
